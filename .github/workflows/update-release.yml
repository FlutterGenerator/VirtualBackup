name: Build & Publish APK (Debug)

on:
  push:
    tags:
      - '*'

jobs:
  build:
    permissions:
      contents: write
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: 17
          distribution: temurin

      - name: Make Gradle executable
        run: chmod +x ./gradlew

      - name: Build Debug APKs (arch32 + arch64)
        run: ./gradlew assembleDebug

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        if: github.ref_type == 'tag'
        with:
          generate_release_notes: true
          prerelease: false
          files: |
            app/build/outputs/apk/arch32/debug/app-arch32-debug.apk
            app/build/outputs/apk/arch64/debug/app-arch64-debug.apk
